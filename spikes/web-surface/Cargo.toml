[package]
name = "web-surface"
version = "0.1.0"
edition = "2024"

[dependencies]
tao = "0.35.3"
wry = "0.55.1"

# Standalone crate — keep out of any parent workspace (smtc-spike precedent).
[workspace]
